{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(cppvsdbg) CMake Launch",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "stopAtEntry": false,
            "requireExactSource": false,
            "console": "integratedTerminal",
            "cwd": "${workspaceRoot}"
        }
    ]
}